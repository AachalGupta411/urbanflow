# Remote state backend is configured per environment in environments/<env>/backend.tf.
# Run Terraform from the target environment directory, e.g.:
#   cd environments/dev && terraform init && terraform plan
